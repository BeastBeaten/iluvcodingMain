package com.mainSite.app.web.action.entry.mobile;


import com.mainSite.app.web.action.BaseController;
import net.sf.json.JSONObject;
import org.apache.commons.lang3.StringUtils;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;

import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

/**
 *
 * 移动端入口Controller
 * @author zhouqing
 */
@Controller
@RequestMapping("/mobile")
public class MobileCommonController extends BaseController{

    //private static Logger logger = LoggerFactory.getLogger(MobileCommonController.class);

}
